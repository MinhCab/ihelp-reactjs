import Header from './Header';
import Form from './Form'
function Register() {
    return (
        <div>
            <Header />
            <Form />
        </div>

    );
}

export default Register;
